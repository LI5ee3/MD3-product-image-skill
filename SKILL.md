---
name: md3-product-main-image
version: 5.2
description: >
  GPT-image-only workflow for creating MD3 e-commerce product main images.
  The skill applies strict constraints only to asset fidelity, canvas, information layout,
  master/variant consistency, and basic readability, while keeping composition, palette,
  spatial treatment, and visual mood open to independent design judgment.
---

# MD3 Product Main Image Skill v5.2

## 设计原则

本 Skill 将规则分为两类：

### A. 强约束
必须严格执行：

- 画布规格
- Logo 保护
- Logo 位置与尺寸
- 产品 PNG 保护
- 产品名称与版本文字
- 信息区范围与基本对齐
- 禁止额外文字与装饰内容
- ORIGINAL MASTER 锁定
- SKU 替换关系
- 单回合独立候选输出

### B. 创意空间
不做具体预设：

- 背景颜色
- 冷暖
- 明度风格
- 构图类型
- 场景形式
- 是否使用承托结构
- 几何元素组合
- 视觉情绪
- 空间关系
- 光影表现

核心原则：

**硬约束明确，创意约束最小化。**

---

# 工作状态

1. CREATE_MASTER_OPTIONS
2. WAIT_FOR_MASTER_SELECTION
3. REPLACE_VARIANT
4. BUILD_SKU_SET

---

# STATE 1 — CREATE_MASTER_OPTIONS

适用于：

- 新产品首次创建主图
- 当前产品尚无正式母版
- 用户要求重新生成新的母版候选

## 单回合只生成 1 张候选

普通聊天模式下：

- 当前回合只生成 1 张独立母版候选
- 一张图片只包含一个完整方案
- 不得把多个候选合并在同一图片中

禁止：

- 拼版
- 五宫格
- Contact Sheet
- Collage
- Grid
- Multi-panel
- 图片内部候选编号

如果用户需要更多候选，必须通过后续独立回合继续生成。

## 候选之间独立设计

每个候选都从相同原始输入重新开始：

BRAND LOGO + PRODUCT PNG + PRODUCT NAME + VERSION TEXT
→ INDEPENDENT DESIGN

新候选不得以前一个候选作为设计起点。

不要默认继承前一个候选的：

- 构图
- 配色
- 明度关系
- 产品位置
- 产品视觉尺寸
- 背景结构
- 空间关系
- 光影
- 视觉气质

每个候选均根据当前产品重新进行设计判断。

候选之间允许自然出现相似点。

不要为了制造差异而强行改变颜色或结构。

生成当前候选后：

STOP

等待用户继续生成下一候选或选择母版。

---

# STATE 2 — WAIT_FOR_MASTER_SELECTION

仅在用户明确选择时锁定母版。

例如：

- “方案3”
- “第三张作为母版”
- “选第3个”
- “方案3，就用这个”

然后：

- MASTER_SELECTED = selected original candidate
- MASTER_APPROVED = true

选中的原始候选图成为唯一 ORIGINAL MASTER。

禁止：

- 混合其他候选
- 重做一个“相似母版”
- 使用其他候选作为 SKU 来源

---

# STATE 3 — REPLACE_VARIANT

适用于：

- Image A = ORIGINAL MASTER
- Image B = 当前同款 SKU 产品 PNG

这是锁定母版后的产品替换。

不是重新设计海报。

所有 SKU 必须直接从：

ORIGINAL MASTER + CURRENT SKU

生成。

正确：

- ORIGINAL MASTER + SKU 2
- ORIGINAL MASTER + SKU 3
- ORIGINAL MASTER + SKU 4

错误：

- MASTER → SKU 2 → SKU 3 → SKU 4

禁止使用前一个 SKU 变体作为下一个 SKU 的来源。

---

# STATE 4 — BUILD_SKU_SET

适用于一次上传多个同款 SKU。

流程：

1. 指定母版 SKU
2. 使用母版 SKU 生成第 1 张独立候选
3. 停止
4. 用户需要时继续逐张生成其他候选
5. 用户选择 ORIGINAL MASTER
6. 锁定 ORIGINAL MASTER
7. 其他 SKU 全部直接从 ORIGINAL MASTER 生成

未选择母版前，不得生成其他 SKU。

---

# 画布规则

所有输出必须：

- 比例：3:4
- 目标尺寸：1200 × 1600 px

适用于：

- 母版候选
- 正式母版
- SKU 变体
- 批量 SKU

不得先生成其他比例后裁切。

---

# 上传素材保护

上传的品牌 Logo 与产品 PNG 是受保护源素材。

设计必须适配素材。

不得为了构图而修改素材本身。

---

# 品牌 Logo 原子化规则

上传 Logo 必须作为一个不可拆分的完整图形处理。

Logo 内所有内容均属于 Logo 本体，包括：

- 图形标
- Symbol
- Emblem
- Wordmark
- 品牌文字
- 字形
- 字距
- 字重
- 内部比例
- 内部对齐
- 颜色
- 透明区域
- 边缘细节

Logo 内嵌文字不是可编辑文字。

禁止：

- OCR 后重新打字
- 重新生成 Wordmark
- 用普通文字替代 Logo 内文字
- 修改字形
- 修改字距
- 修改字重
- 拆分 Logo
- 重建 Logo
- 使用相似 Logo 替代

只允许：

- 整体等比例缩放
- 整体定位

---

# Logo 位置规则

Logo 只能位于左上安全区。

1200 × 1600 px 画布：

- 距左边缘至少 60 px
- 距上边缘至少 80 px
- Logo 必须完整位于画布内

Logo 位置不是候选设计变量。

---

# Logo 尺寸规则

Logo 最大包围框：

- 最大宽度：220 px
- 最大高度：100 px

必须保持原始宽高比。

禁止：

- 拉伸
- 压缩
- 裁切
- 变形

候选中 Logo 视觉尺寸保持一致。

母版选定后：

- Logo 位置锁定
- Logo 宽度锁定
- Logo 高度锁定

---

# 信息区规则

品牌 Logo、产品名称和版本文字组成稳定信息区。

## 位置

- Logo 位于左上安全区
- 产品名称位于 Logo 下方
- 产品名称与版本文字必须位于主图上半部分
- 不得下沉到画面中部或下半部分
- 主图中部和下部主要留给产品展示

## 对齐

优先：

- 产品名称左起点与 Logo 左边缘形成统一视觉左轴
- 版本文字左起点与产品名称左起点对齐

若 Logo 形态导致机械对齐不自然：

- 允许轻微视觉补偿
- 但必须保持统一视觉左轴
- Logo 不得离开左上安全区

## 间距

Logo → 产品名称：

- 保持合理垂直留白
- 不得拥挤
- 不得远到失去信息组关系

产品名称 → 版本文字：

- 保持清晰层级
- 保持稳定间距

文字区不得侵入产品核心展示区。

## 长标题

产品名称较长时：

- 优先一行
- 必要时最多两行
- 不得从单词中间断开
- 可适度缩小字号
- 整个信息区仍须保持在上半部分

## 母版锁定

选定 ORIGINAL MASTER 后锁定：

- 标题位置
- 版本位置
- Logo / 标题左轴关系
- Logo → 标题间距
- 标题 → 版本间距
- 整个信息区几何结构

所有 SKU 必须保持一致。

---

# 产品保护规则

上传产品 PNG 是产品真实外观的唯一权威来源。

禁止：

- 重新绘制产品
- 用相似型号替代
- 修改产品颜色
- 修改轮廓
- 修改结构
- 修改机身 / 表壳 / 表带 / 耳机 / 充电盒形状
- 修改按钮 / 表冠 / 孔位 / 接口
- 修改屏幕比例
- 修改材质
- 添加不存在的硬件
- 删除真实细节

只允许：

- 等比例缩放
- 调整位置
- 构图摆放
- 自然接触阴影
- 柔和环境阴影
- 必要的轻微轮廓分离光

---

# 画面内容规则

只允许出现：

1. 上传品牌 Logo
2. 精确产品名称
3. 精确版本文字
4. 上传产品 PNG
5. MD3 背景设计元素
6. 自然光影

产品本身真实存在的屏幕文字或印刷内容可以保留。

禁止新增：

- 功能卖点
- 参数
- 促销信息
- 折扣标签
- 认证
- 额外 Logo
- 额外图标
- 徽章
- 装饰性英文
- 平台贴纸
- 虚构 UI 元素

---

# 文字规则

产品名称：

- 现代中性无衬线视觉
- Material / Google Sans / Roboto-like
- Bold 700
- 清晰醒目
- 优先一行
- 最多两行
- 不得过度缩小

版本文字：

- Medium 500
- 清晰可读
- 视觉层级低于产品名称
- 不得过小
- 不得像脚注

---

# MD3 设计语言

使用 Material Design 3 作为设计语言参考，而不是固定模板。

根据当前产品与当前构图，自由决定：

- 空间关系
- 几何组织
- Tonal 组织
- 层级关系
- 视觉节奏

不要求使用任何特定 MD3 元素。

不要求采用任何固定产品展示结构。

重点只保证：

- 信息层级清晰
- 留白关系合理
- Tonal 关系协调
- 几何关系具有整体性
- 产品与设计元素之间形成合理视觉关系

不要做成 Android App UI。

---

# 场景与展示形式

不预设固定的产品展示形式。

产品与背景之间的空间关系，应根据当前产品、构图与 MD3 设计逻辑独立决定。

任何场景元素、承托结构或空间元素都不是必需项。

只有在确实有助于当前构图时才使用。

不要因为过去的方案、常见设计习惯或产品类别，自动沿用某种展示结构。

---

# 视觉气质自由规则

不预设固定视觉情绪。

不预设：

- 固定明度倾向
- 固定冷暖
- 固定情绪
- 固定视觉性格

具体视觉气质由：

- 当前产品
- 产品材质
- 产品颜色
- 当前构图
- 品牌视觉
- 缩小显示时的识别需求

共同决定。

只要求：

- 产品清晰
- 层级合理
- 构图完整
- 符合 MD3 设计语言

---

# 自适应配色系统

不设置固定背景色。

不设置“产品颜色 → 背景颜色”对照表。

不设置候选色彩家族配额。

每个新产品、每个候选、每个 SKU 都根据当前实际情况重新评估配色。

综合考虑：

- 产品整体明度
- 产品饱和度
- 产品材质
- 产品视觉重量
- 产品局部强调色
- 品牌视觉
- 产品与背景分离
- 当前构图
- MD3 Tonal Balance
- 缩小显示时的识别度

核心原则：

**通过明度、色调层级与轮廓分离保证产品清晰；具体色相自由。**

不要机械沿用：

- 前一个候选的背景色
- 前一个 SKU 的背景色
- 过去其他产品的背景色

也不要为了“看起来不同”而强行改变色相。

---

# 深色 / 浅色 / 彩色产品

## 深色产品

只检查：

- 产品轮廓是否清楚
- 产品与背景是否有足够分离
- 缩小显示时是否容易识别

不限定：

- 背景色相
- 冷暖
- 色彩家族

## 浅色产品

重点检查：

- 产品是否融入背景
- 轮廓分离
- 明度分离
- Tonal 分层

不限定背景色相。

## 彩色产品

不要机械：

- 使用同色背景
- 使用互补色背景
- 根据颜色名称套用固定方案

由模型根据当前产品和构图自由判断背景配色。

不设置固定背景颜色列表。

---

# 缩略图可读性

在缩小显示时：

- 产品主体仍应容易识别
- 产品与背景保持清晰分离
- 产品名称保持基本可读
- 信息层级保持明确
- 构图保持完整

这里只限制可读性与识别度。

不规定：

- 具体构图形式
- 具体场景形式
- 具体展示套路

---

# SKU 自适应配色

REPLACE_VARIANT 时：

锁定：

- ORIGINAL MASTER 构图
- Logo
- 信息区
- 产品展示区域
- 产品视觉尺寸逻辑
- 产品视觉中心
- 背景几何形状
- 背景几何大小
- 背景几何位置

允许当前 SKU 自适应：

- 背景主色
- MD3 几何填充色
- Tonal Surface 配色
- 阴影强弱
- 必要的轻微轮廓分离光
- 文字明暗（仅在可读性需要时）

每个 SKU 都重新评估自己的配色。

不要继承前一个 SKU 的色相偏好。

目标：

**锁定结构，自适应配色。**

---

# REPLACE_VARIANT 版式锁定

严格保留 Image A：

- Logo 样式
- Logo 大小
- Logo 位置
- 产品名称内容
- 产品名称大小
- 产品名称位置
- 版本文字内容
- 版本文字大小
- 版本文字位置
- 信息区对齐
- 信息区间距
- 产品展示区域
- 产品视觉尺寸逻辑
- 产品视觉中心
- 背景几何形状
- 背景几何大小
- 背景几何位置
- 整体构图
- 基本光源方向

不得重新设计海报。

---

# QA

每张图检查：

- 严格 3:4
- 目标尺寸 1200 × 1600
- Logo 与上传素材一致
- Logo 内文字没有被重新排版
- Logo 位于左上安全区
- Logo 尺寸合规
- 产品与上传 PNG 一致
- 产品未被重绘或改色
- 产品名称精确
- 版本文字精确
- 产品名称和版本位于上半部分
- 信息区对齐与间距合理
- 文字区未侵入产品核心展示区
- 无额外文字、图标或徽章
- 产品在缩小显示时容易识别
- 产品与背景分离清晰
- MD3 是设计语言而不是固定模板
- 配色不是来自固定颜色表
- 视觉气质不是来自固定情绪模板
- 展示形式不是来自固定产品模板

母版候选额外检查：

- 当前回合只输出 1 张独立候选
- 没有拼版
- 当前候选从原始输入重新独立设计
- 没有把前一个候选作为设计起点

变体额外检查：

- 使用 ORIGINAL MASTER
- Logo 未移动或缩放
- 信息区未移动或重排
- 产品视觉中心与尺寸逻辑保持一致
- 背景几何形状、大小、位置未改变
- 当前 SKU 配色被独立重新评估
