【输入素材】

品牌 Logo：
使用我上传的品牌 Logo。

产品：
使用我上传的当前产品 PNG。

产品名称：
{{产品名称}}

版本文字：
{{版本文字}}

━━━━━━━━━━━━━━━━━━
【任务】
━━━━━━━━━━━━━━━━━━

请为当前产品生成 5 个电商产品主图母版候选，
供我选择其中一个作为后续同款不同 SKU 的唯一 ORIGINAL MASTER。

当前阶段只进行母版探索。

不要提前生成其他 SKU。

━━━━━━━━━━━━━━━━━━
【输出】
━━━━━━━━━━━━━━━━━━

5 个候选必须是 5 张独立图片。

不得生成：

- 五宫格
- 拼版
- Contact Sheet
- Collage
- Grid
- Multi-panel
- 一张图中的多个方案

每张图片只包含一个完整候选。

图片内部不得出现方案编号。

如果当前界面无法可靠一次输出 5 张独立图片：

先只生成方案 1，然后停止。

不得通过拼版解决。

━━━━━━━━━━━━━━━━━━
【候选必须独立设计】
━━━━━━━━━━━━━━━━━━

5 个候选不是连续迭代关系。

每个候选都从以下相同原始输入重新开始：

品牌 Logo + 产品 PNG + 产品名称 + 版本文字

生成新候选时，
不要以前一个候选作为设计起点。

不要默认继承前一个候选的：

- 配色
- 明度结构
- 产品位置
- 产品视觉尺寸
- 背景几何
- 空间节奏
- 光影关系
- 视觉气质

每个候选都独立判断当前产品最适合的设计。

如果独立判断后部分结果相近，这是允许的。

不要为了“不同”而强行使用不合适的颜色或构图。

━━━━━━━━━━━━━━━━━━
【画布】
━━━━━━━━━━━━━━━━━━

严格：

- 3:4
- 目标尺寸 1200 × 1600 px

不得先生成其他比例后裁切。

━━━━━━━━━━━━━━━━━━
【Logo】
━━━━━━━━━━━━━━━━━━

上传 Logo 是一个不可拆分的受保护完整图形。

Logo 内所有内容都属于 Logo 本体，
包括图形标、Wordmark、文字、字形、字距、内部比例、颜色和透明区域。

Logo 内文字不是可编辑文字。

禁止：

- OCR 后重新打字
- 重新生成 Wordmark
- 修改字形或字距
- 拆分 Logo
- 重绘 Logo
- 使用相似 Logo 替代

只允许整体等比例缩放和定位。

Logo 只能位于左上安全区。

1200 × 1600 px：

- 距左至少 60 px
- 距上至少 80 px

Logo 最大包围框：

- 220 × 100 px

保持原始宽高比。

5 个候选中 Logo 视觉尺寸保持一致。

━━━━━━━━━━━━━━━━━━
【信息区】
━━━━━━━━━━━━━━━━━━

Logo、产品名称、版本文字组成稳定信息区。

必须：

- 产品名称位于 Logo 下方
- 产品名称和版本文字始终位于主图上半部分
- 主图中部和下部主要留给产品展示
- 产品名称左起点优先与 Logo 左边缘形成统一视觉左轴
- 版本文字与产品名称左对齐
- Logo 与标题保持合理垂直留白
- 标题与版本保持稳定层级间距
- 文字区不得侵入产品核心展示区

长标题：

- 优先一行
- 必要时最多两行
- 不得从单词中间断开
- 可以适度缩小字号
- 不得把信息区下推到画面中部

━━━━━━━━━━━━━━━━━━
【产品保护】
━━━━━━━━━━━━━━━━━━

上传产品 PNG 是产品真实外观的唯一权威来源。

禁止：

- 重新绘制
- 修改颜色
- 修改型号
- 修改轮廓
- 修改结构
- 修改材质
- 修改按钮 / 表冠 / 孔位 / 接口
- 修改屏幕比例
- 添加或删除真实硬件细节

只允许：

- 等比例缩放
- 调整位置
- 构图
- 自然阴影
- 必要的轻微轮廓分离光

━━━━━━━━━━━━━━━━━━
【画面内容】
━━━━━━━━━━━━━━━━━━

只允许：

1. 上传品牌 Logo
2. 产品名称：{{产品名称}}
3. 版本文字：{{版本文字}}
4. 上传产品 PNG
5. MD3 背景设计元素
6. 自然光影

禁止增加：

- 卖点
- 参数
- 促销文字
- 标签
- 认证
- 额外 Logo
- 图标
- 徽章
- 装饰性英文
- 虚构 UI

━━━━━━━━━━━━━━━━━━
【文字】
━━━━━━━━━━━━━━━━━━

产品名称：

- 现代中性无衬线视觉
- Material / Google Sans / Roboto-like
- Bold 700
- 清晰醒目
- 优先一行
- 最多两行

版本文字：

- Medium 500
- 清晰可读
- 视觉层级低于产品名称
- 不得像脚注

━━━━━━━━━━━━━━━━━━
【MD3 设计语言】
━━━━━━━━━━━━━━━━━━

使用 Material Design 3 作为设计语言参考。

不要把 MD3 理解成固定模板。

不要要求每张图必须同时出现固定的：

- 圆弧
- 圆形
- 渐变
- 底座
- Tonal Surface
- 某种几何组合

根据当前产品和构图，自由选择适合的 MD3 设计元素。

重点保证：

- 层级清晰
- 留白合理
- 空间关系自然
- Tonal 组织合理
- 几何语言协调
- 适合电商商品卡片

不要做成 Android App UI。

━━━━━━━━━━━━━━━━━━
【视觉气质】
━━━━━━━━━━━━━━━━━━

不预设固定视觉情绪。

不要因为 Skill 中的风格词，
让 5 个候选自动收敛到同一种：

- 明度
- 冷暖
- 配色
- 情绪
- 视觉性格

具体视觉气质由当前产品、材质、颜色、构图、品牌视觉和电商展示需求共同决定。

只要求最终结果：

- 产品清晰
- 层级合理
- 构图完整
- 符合 MD3 设计语言
- 适合电商商品卡片

━━━━━━━━━━━━━━━━━━
【自适应配色】
━━━━━━━━━━━━━━━━━━

不设置固定背景色。

不设置“产品颜色 → 背景颜色”对照表。

不设置候选色彩家族配额。

每个候选独立重新评估配色。

综合考虑：

- 产品明度
- 产品饱和度
- 材质
- 视觉重量
- 局部强调色
- 品牌视觉
- 产品与背景分离
- 当前构图
- MD3 Tonal Balance
- 缩略图识别度

通过明度、色调层级和轮廓分离保证产品清晰。

具体色相自由。

不要机械沿用前一个候选的背景色。

也不要为了制造差异而强行改变色相。

━━━━━━━━━━━━━━━━━━
【深色 / 浅色 / 彩色产品】
━━━━━━━━━━━━━━━━━━

深色产品：
只要求产品与背景具有足够分离，
避免缩略图中轮廓消失。
不限定背景色相。

浅色产品：
只要求避免产品融入背景，
保证轮廓、明度和 Tonal 分离。
不限定背景色相。

彩色产品：
不要机械使用同色背景，
也不要机械套用互补色。
根据当前产品和构图自由判断配色。
不设置固定背景颜色列表。

━━━━━━━━━━━━━━━━━━
【电商缩略图】
━━━━━━━━━━━━━━━━━━

优先考虑 Ozon 等电商商品列表。

缩略图状态下：

- 产品容易识别
- 标题保持可读
- 产品与背景分离清楚
- 信息层级明确
- 构图完整

━━━━━━━━━━━━━━━━━━
【5 个候选差异】
━━━━━━━━━━━━━━━━━━

候选差异主要来自：

- 产品位置
- 产品视觉尺寸
- 信息区与产品关系
- 留白
- MD3 几何
- 曲线和圆弧
- Tonal Surface 组织
- 空间层次
- 光影关系
- 整体视觉节奏

差异不得仅靠换背景色产生。

━━━━━━━━━━━━━━━━━━
【最终 QA】
━━━━━━━━━━━━━━━━━━

检查：

- 严格 3:4
- 目标尺寸 1200 × 1600
- 一张图只有一个候选
- Logo 与上传原图一致
- Logo 位于左上安全区
- Logo 内文字未被重新生成
- 产品名称与版本位于上半部分
- 信息区对齐与间距合理
- 产品与上传 PNG 一致
- 产品未被重新绘制或改色
- 无额外文字、图标、徽章
- 产品在缩略图中清晰
- 产品与背景分离合理
- 每个候选是独立设计
- 没有以前一个候选作为设计起点
- 配色没有使用固定颜色模板
- 视觉气质没有使用固定情绪模板

请开始生成母版候选。

如果当前界面无法可靠一次输出 5 张独立图片，
先只生成方案 1。
